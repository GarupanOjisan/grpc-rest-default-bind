# grpc-rest-default-bind

## what's this?

This protoc plugin creates gateways without additional config like grpc-gateway.

## installation

```bash
$ go get -u github.com/garupanojisan/protoc-gen-restize
```